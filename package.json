{
  "name": "campus-reconnect",
  "version": "1.0.0",
  "description": "A campus lost & found platform with secure verification and reward incentives",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest --testPathPattern=tests/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
